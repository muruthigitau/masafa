import React from "react";

const DocMessages = ({ config, data }) => {
  // Render messages tab content here
  return (
    <div className="min-h-screen flex items-center justify-center">
      <h2>Messages</h2>
      {/* Add your messages tab content here */}
    </div>
  );
};

export default DocMessages;
